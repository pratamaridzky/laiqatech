import { SubAreaDataSource } from './sub-area-data-source';

describe('SubAreaDataSource', () => {
  it('should create an instance', () => {
    expect(new SubAreaDataSource()).toBeTruthy();
  });
});
