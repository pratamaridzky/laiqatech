import { CurrencyDataSource } from './currency-data-source';

describe('CurrencyDataSource', () => {
  it('should create an instance', () => {
    expect(new CurrencyDataSource()).toBeTruthy();
  });
});
