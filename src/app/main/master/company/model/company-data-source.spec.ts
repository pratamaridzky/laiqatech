import { CompanyDataSource } from './company-data-source';

describe('CompanyDataSource', () => {
  it('should create an instance', () => {
    expect(new CompanyDataSource()).toBeTruthy();
  });
});
